/**
 * @lumina/ui hooks
 * Custom React hooks for the Lumina platform
 *
 * NOTE: Desktop-only hooks (useMeetingModeStateMachine) are NOT exported here.
 * Import them from '@lumina/ui/desktop' instead to avoid pulling in
 * @mediapipe/tasks-vision which is not available in web builds.
 */

// Empty export to make this a valid ES module
// Web-safe hooks can be added here in the future
export {};
