# Lumina - Current Status & Next Steps

**Date:** December 19, 2024
**Last Updated:** After Supabase credentials setup

---

## Completed Tasks

| Task | Status |
|------|--------|
| Supabase project created | DONE |
| `apps/web/.env.local` created | DONE |
| `apps/desktop/.env` created | DONE |

---

## Next Step: Run Database Migration

### Instructions

search how you can do this and please do that. if not possible then only ask me.

1. Go to your Supabase dashboard: https://supabase.com/dashboard
2. Select your project (acvmkigubzldhpyrlail)
3. Click **"SQL Editor"** in the left sidebar
4. Click **"New query"** button
5. Open this file in your editor: `lumina/supabase/migrations/001_initial_schema.sql`
6. Copy the **entire** contents
7. Paste into the Supabase SQL Editor
8. Click **"Run"** (or press Ctrl+Enter / Cmd+Enter)
9. Should see: "Success. No rows returned"

### What the Migration Creates

| Table | Purpose |
|-------|---------|
| `organizations` | Multi-tenancy - each company is an org |
| `org_members` | User roles: admin, manager, employee |
| `wellness_data` | Blink metrics synced from desktop app |
| `org_alerts` | Admin notifications for concerning metrics |

**Also creates:**
- Row Level Security (RLS) policies for data isolation
- Indexes for fast queries
- Helper functions (`get_user_org`, `is_org_admin`)

---

## After Migration: Optional Google OAuth Setup

The redirect URL for Google OAuth is:
```
https://acvmkigubzldhpyrlail.supabase.co/auth/v1/callback
```

OAuth client created
The client ID can always be accessed from Clients tab under Google Auth Platform.

OAuth access is restricted to the test users  listed on your OAuth consent screen
Client ID
[REDACTED-CLIENT-ID].apps.googleusercontent.com
Starting in June 2025, you will no longer be able to view or download the client secret once you close this dialog. Make sure you have copied or downloaded the information below and securely stored it.
Client secret
[REDACTED-CLIENT-SECRET]
Creation date
December 19, 2025, 5:18:06 PM GMT+5
Status
 Enabled

C:\Users\sidro\Downloads\client_secret_[REDACTED-CLIENT-ID].apps.googleusercontent.com.json 

### Steps (Optional - Magic Link Still Works)

1. Go to https://console.cloud.google.com
2. Create a new project or use existing
3. Go to **APIs & Services > Credentials**
4. Click **Create Credentials > OAuth Client ID**
5. Choose "Web application"
6. Add Authorized redirect URI: `https://acvmkigubzldhpyrlail.supabase.co/auth/v1/callback`
7. Copy **Client ID** and **Client Secret**
8. In Supabase dashboard: **Authentication > Providers**
9. Enable Google and paste the credentials

**You can skip this** - magic link auth will work without it.

---

## After Migration: Test the Web App

```bash
cd lumina
pnpm dev:web
```

Open http://localhost:3000

You should see the landing page with login/signup options.

---

## What Claude Will Do After You Confirm Migration

Once you confirm the migration ran successfully, I will:

1. **Wire up real authentication**
   - Replace mock auth in login/join pages
   - Implement magic link flow
   - Implement Google OAuth (if enabled)

2. **Connect dashboard to real data**
   - Replace mock data with Supabase queries
   - Add real-time updates
   - Implement loading states

3. **Test and debug desktop camera**
   - Help troubleshoot any MediaPipe issues
   - Verify blink detection works

---

## Quick Reference

### Supabase Dashboard
- URL: https://supabase.com/dashboard
- Project: acvmkigubzldhpyrlail

### Environment Files Created
- `lumina/apps/web/.env.local` - Web app Supabase config
- `lumina/apps/desktop/.env` - Desktop app Supabase config

### Migration File
- `lumina/supabase/migrations/001_initial_schema.sql`

---

## Reply With

Just tell me: "Migration done" or paste any errors you see.
